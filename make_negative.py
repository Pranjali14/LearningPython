def make_negative( number ):
    if(number <= 0) :
        return number;
    else :
        return -number;
        
// using ternary condition

def make_negative( number ):
    return number if (number <= 0) else -number;
